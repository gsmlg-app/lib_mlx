# Run with `dart run ffigen --config ffigen.yaml`.
name: LibMlxBindings
description: |
  Bindings for `src/lib_mlx.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/lib_mlx_bindings_generated.dart'
headers:
  entry-points:
    - 'src/lib_mlx.h'
  include-directives:
    - 'src/lib_mlx.h'
comments:
  style: any
  length: full
